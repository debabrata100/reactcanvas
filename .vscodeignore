# Everything is bundled into dist/ — keep the .vsix minimal.
.vscode/**
.vscode-test/**
.github/**
src/**
out/**
node_modules/**
examples/**
images/preview.gif
esbuild.mjs
eslint.config.mjs
tsconfig.json
.gitignore
.nvmrc
**/*.map
**/*.ts
**/*.vsix
npm-debug.log*

# Keep:
# dist/** (bundle + esbuild.wasm), images/icon.png, README.md, CHANGELOG.md,
# LICENSE, package.json
