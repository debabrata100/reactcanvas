# Images

- `icon.png` — 128×128 extension icon referenced by `package.json` (`"icon"`).
  If you replace it, keep the name and size (PNG, 128×128) so Marketplace
  packaging keeps working.
- `preview.gif` — (placeholder, not yet added) usage GIF referenced by the
  README. Record a short clip of editing a `.jsx` file with the live preview
  open and save it here.
